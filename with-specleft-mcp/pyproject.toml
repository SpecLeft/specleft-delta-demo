[project]
name = "with-specleft-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.5"
dependencies = [
  "fastapi",
  "pydantic",
  "sqlalchemy",
  "specleft",
  "pytest",
  "requests",
  "httpx",
]
